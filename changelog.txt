CHANGELOG
=========
Newest entries on top. Each entry holds the longer detail that would
otherwise go in the git commit body; commit subjects stay to one line.


Governance — binding rules for RTM, ADRs, git workflow, dates   (2026-07-22)
----------------------------------------------------------------------------
docs/process/GOVERNANCE.md: authoritative, RFC-2119-style rules binding all
contributors (human and AI, on every fleet machine). Consolidates and
supersedes the scattered conventions in RTM.md / adr/README.md / docs/README.

- RTM (§1): requirements are living artifacts. Closed status vocabulary
  with exact definitions — adds `in-progress` and `invalidated` (a change
  touching a verified requirement without re-verification evidence in the
  same commit MUST downgrade it); `review` frozen as legacy-seed-only.
  Update triggers (implementation/verification/invalidation/scope/gates/
  baseline), prohibitions (no evidence → no implemented-verified; no
  drive-by edits; no wholesale regeneration), wording rules (no hedging).
- ADRs (§2): decision content of an accepted ADR is immutable; change =
  supersession protocol (new ADR + Supersedes links + index update, same
  commit). Amendable: status, implementation-status line, cross-links,
  typos. Numbers never reused. Required metadata enumerated.
- Git (§3): mandatory session-start fetch/compare protocol before any file
  edit; ff-only when behind; STOP + escalate on divergence; force-push and
  history rewrites prohibited; user performs all commits/pushes (D4);
  per-file-class merge-conflict rules (reports keep both; changelog keeps
  both entries; rtm.csv evidence wins; source logic → escalate); hand-off
  rule: work moves between machines only as pushed commits.
- Dates (§4): absolute ISO dates only, taken from the system clock at
  writing time; filing vs capture date distinction; no back/future-dating.
- RTM.md, adr/README.md, docs/README.md updated to defer to GOVERNANCE.md.
- docs/README.md conventions: cross-references must name the target document
  (bare §N only for same-document sections); specs recorded as client-received
  originals (preserve content + filename, never edit/rename — Markdown
  conversion is a deferred future task).


M1/WP2 — device regression harness (built; baseline run pending)   (2026-07-21)
-------------------------------------------------------------------------------
The security freeze for M1: scripted, headless gating checks so the WP5/WP6
engine refactors can be proven not to regress the two Phase-3 bypasses.

- scripts/e2e/ — new harness:
  - lib.sh: config + helpers (dumpsys resumed-activity focus detection,
    fraction-based PIN-pad taps, uiautomator content-desc probing, a11y
    rebind, Clock resolution). Parameterized by device serial + APP_ID
    (ready for WP4 flavor suffixes).
  - smoke_core / ov3_fast_switch / ov4_rapid_relaunch / f3_self_gate:
    one check per gating property. OV-4 defends the F4 fast-relaunch
    bypass; f3_self_gate defends the F3 resume/FR-108 bypass (asserted via
    the "Open vault" content-desc that appears only in the unlocked App
    List, since the self-gate and App List are the same MainActivity).
  - setup_device.sh (provision: install, PIN, a11y, protect Clock via UI)
    and run_all.sh (runs all checks N times, emits a markdown report block
    for docs/reports/campaigns/).
- Validation: deterministic helpers proven live on the 2012-host Pixel_5
  API 30 emulator (screen size 1080x2340, dumpsys focus parse, clock
  resolution, a11y read). Full interactive baseline run is PENDING on a
  healthy host — the 2012 emulator SystemUI ANR-wedged this session
  (bad color buffer handle / swiftshader on the old GPU). Run the first
  baseline on the NucBox G5 and file it in docs/reports/campaigns/; that
  run is the WP2 exit criterion.
- All scripts pass bash -n; harness README documents assertion strategy,
  validation status, and device/fleet notes.


Docs — fold NucBox provisioning deviations into setup guide   (2026-07-21)
--------------------------------------------------------------------------
Follow-up to the NucBox G5 fleet report (below): captured its three setup
deviations in the reproducible guide and resolved the ADR-014 fleet note.

- docs/process/DEV_ENVIRONMENT_SETUP.md: fresh-host reboot gotcha (batch
  /gradle won't launch until a reboot), cmdline-tools may be missing from
  the GUI SDK (add from official zip, short-path extraction to dodge
  MAX_PATH truncation, verify SHA-256), build-tools "35.x or newer"
  (36.0.0 verified green).
- ADR-014 fleet note trimmed to point at the reports fleet index as the
  live source of truth; records the resolved outcome (NucBox boots x86_64
  under WHPX → full local matrix, no virtualization fallback).


Report — NucBox G5 fleet provisioning outcome   (2026-07-21)
------------------------------------------------------------
First fleet evidence record for the NucBox G5 (ADR-014 verification
fleet). Host provisioned per docs/process/DEV_ENVIRONMENT_SETUP.md;
outcome: fleet-ready.

- docs/reports/fleet/2026-07-21_fleet-nucbox-g5.md — NEW immutable
  report. WHPX acceleration usable (accel-check → accel 0);
  assembleDebug green (8m56s → app-debug.apk 41.72 MB); x86_64
  emulator boots (matrix_api33: sdk=33, abi=x86_64). AVDs
  matrix_api26/29/33/35 created; API 26+29 google_apis x86_64 system
  images installed.
- docs/reports/README.md — fleet index row for NucBox G5 updated to
  link the report (was "pending first fleet report").
- Deviations noted for the setup guide: cmd.exe unreachable until a
  reboot; cmdline-tools absent from the GUI SDK (added from the
  official zip, SHA-256 verified; MAX_PATH extraction gotcha);
  build-tools 36.0.0 vs the guide's 35.x. Build green regardless.
- Scope note: matrix is now locally EXECUTABLE, not yet VERIFIED —
  NFR-COMP-001 evidence still awaits the WP2 regression harness + CI
  matrix. RTM rows left unchanged (status promotion deferred to the
  gate/lead).


Docs — documentation map, reports tree, fleet onboarding   (2026-07-20)
-----------------------------------------------------------------------
Multi-machine support: the verification fleet (2012 host, NucBox G5,
Moto G 2025) shares state only through git, so placement rules and
evidence records become committed documents.

- docs/README.md — documentation map: what each area holds, where a new
  document belongs, conventions (formats, naming, changelog/RTM duties),
  and the git-is-the-only-shared-channel rule for fleet machines.
- docs/reports/ — NEW top-level evidence tree, distinct from specs and
  plans: dated, immutable, host-tagged records (YYYY-MM-DD_topic_host.md,
  append-only so concurrent machines never merge-conflict). Categories:
  fleet/, campaigns/, gates/, benchmarks/, security/, release/ — created
  on first use; index + rules in docs/reports/README.md. Test PLANS stay
  in docs/testing/; execution RESULTS now go to reports/campaigns/;
  gate reviews to reports/gates/ (M1_PLAN references updated).
- docs/process/DEV_ENVIRONMENT_SETUP.md — reproducible Windows host
  setup (written for NucBox G5 onboarding): WHPX acceleration check
  first, JDK/SDK, env vars, clone, build smoke, AVD matrix, shared
  debug-keystore note for cross-machine upgrade tests.
- ADR-014 fleet note updated: NucBox G5 arrived 2026-07-20 (Win 11,
  N-series CPU — sequential matrix, WHPX check pending); Moto G 2023
  order cancelled, replaced by Moto G 2025 arriving 2026-07-22.
- WP1 exit criterion CLOSED: two consecutive green CI runs on main
  (first attempt hit a GitHub-side Actions incident; re-run clean).


M1/WP1 — CI baseline freeze   (2026-07-19)
------------------------------------------
First work package of the M1 foundation retrofit (plan: docs/process/M1_PLAN.md).
Freezes the current green state in CI before any refactoring begins.

- GitHub Actions workflow (.github/workflows/ci.yml): on every push/PR to main,
  one ubuntu job runs testDebugUnitTest (67 tests) + assembleDebug +
  assembleRelease (R8 smoke — the step that broke in Phase 2) + lint, with
  Gradle caching; test/lint reports and APKs uploaded as artifacts.
- Restored the missing Unix gradlew launcher (repo only had gradlew.bat;
  Linux runners need the shell script), stored executable (100755).
- .gitattributes pins gradlew/*.sh to LF and *.bat to CRLF so the CI runner
  always gets a clean executable script.
- Android lint wired with app/lint-baseline.xml: 55 pre-existing findings
  frozen as accepted debt (39 dependency-version notices → Dependabot in WP4;
  4 errors incl. the opt-in-guarded MissingPermission in
  IntruderCaptureManager — burndown tracked at the M2/M3 gate reviews).
  Only NEW findings fail CI.
- CI badge in README.

Exit criterion still open: two consecutive green runs on unmodified main
(needs this commit pushed).


M0 — New documentation baseline adopted; docs restructured   (2026-07-19)
-------------------------------------------------------------------------
The project was re-baselined onto a new authoritative documentation set
("docs/New docs/", 59 documents) and the repository reorganized around it.
No application code changed in this phase.

Baseline adoption:
- New SRS (18 sections, FR-001..375), NFR corpus (13 sections, 171 NFRs),
  TAS (9 parts), SDS (17 sections incl. detailed component designs), and
  Implementation Strategy v2.0 (7 gated phases) moved into docs/srs, docs/nfr,
  docs/architecture/tas, docs/design/sds, docs/process.
- Full transition analysis in docs/process/MIGRATION_ASSESSMENT.md: SRS
  sections 1-12 are byte-identical to the old set (all implemented work keeps
  its requirement basis); old section 13 (cloud/premium, FR-226..250) removed
  and its FR numbers REUSED by the new Production Readiness section — see
  docs/archive/FR-226-250-RENUMBERING-NOTICE.md. ~80-85% of existing work
  carries forward; recommended path M0..M6 (foundation retrofit before new
  features).

Added:
- docs/process/rtm/rtm.csv — Requirements Traceability Matrix, 546 rows
  (375 FR + 171 NFR), statuses seeded from validation evidence (23
  implemented-verified, 9 partial, 115 review, 399 not-started).
- docs/architecture/adr/ — ADR-001..010 backfilled from TAS §71 plus
  ADR-011 (single-module layout), ADR-012 (SQLCipher-as-Room-equivalent),
  ADR-013 (accessibility detection), ADR-014 (API range: minSdk 26 /
  targetSdk 35, forward-compatible; verification fleet incl. Moto G 2023 +
  NucBox G5 + M1 CI matrix), ADR-015 (Hilt adoption plan; interim rule: no
  new Graph.* lookup sites).
- scripts/extract_docx.py — dependency-free .docx text extraction (used for
  the byte-identity verification; docx stays authoritative, no md mirrors).
- docs/archive/ — superseded docs (old TAS/SDS/diagrams/drafts/FRCS/old
  section 13) with per-file supersession notes.

Changed:
- README rewritten for the new baseline (IS phase table, documentation map).
- Old duplicate SRS sections 1-12 deleted (hash-verified identical; git
  history retains them).
- .gitignore now excludes Office lock files (~$*).
- PHASE4_PLAN/PHASE4_TEST_PLAN relocated (docs/process, docs/testing) and
  marked as M4 design inputs, superseded as the active plan.


Phase 3 — Encrypted vault + intruder selfie   (2026-07-15, uncommitted)
----------------------------------------------------------------------
Encrypted media vault and intruder-selfie capture, plus two security
fixes found during validation.

Added:
- Encrypted vault (vault/ package). SAF import copies each file into an
  AES-256-GCM EncryptedFile blob (UUID-named, perms 0600) under filesDir;
  the human-readable index (name/type/size/date) lives only in the
  SQLCipher DB. Supports in-memory image preview, byte-identical export
  round-trip, secure delete (index row + ciphertext blob), and an opt-in
  "delete originals from device" prompt after import (FR-106..119).
- Intruder selfie (privacy/ package). After a configurable number of
  consecutive failed unlocks, captures a front-camera photo with no
  visible camera UI (CameraX ImageCapture bound to a throwaway
  LifecycleOwner) and writes the JPEG straight into an EncryptedFile.
  Logs an event with timestamp/app/method/attempts/battery/orientation,
  raises a high-priority owner notification, and exposes a history viewer
  (FR-081/082/084/085). Threshold is user-configurable (3/5/10).
- Shared EncryptedFileStore helper (androidx.security.crypto) backing both
  vault blobs and intruder photos.
- Database v2: new intruder_events and vault_items tables via a
  hand-written MIGRATION_1_2.
- Settings: intruder-selfie toggle (requests CAMERA at opt-in) and
  failed-attempt threshold; CAMERA permission + camera uses-feature.

Fixed (security defects found during validation):
- Self-gate was not re-applied on resume: after unlocking once,
  backgrounding and resuming the app returned straight to the app list,
  leaving the vault and intruder log reachable with no re-authentication
  (violated FR-108). Now re-gates on ON_STOP via a SelfLock lifecycle
  observer, skipping config changes and carving out the SAF picker
  round-trip so import/export doesn't bounce the user to the gate.
- Fast-relaunch gating bypass: rapidly relaunching a protected app slid
  its window over the lock screen (which self-finishes via noHistory),
  and the engine deduped the repeat foreground event, so it never
  re-locked and the app's content was exposed. Removed the same-package
  dedup; a protected app with no session now re-locks on every foreground
  event (idempotent singleTop launch, only the first transition logged).

Validation:
- 67/67 JVM unit tests; clean debug and release (R8) builds.
- On-device E2E on headless Pixel_5 (API 30): gating (back / recents /
  fast-switch / rapid-relaunch), vault encrypt-export-delete, reboot
  persistence (data + lockout counter FR-174 + watchdog + a11y rebind),
  force-stop, incoming-call interruption, low-battery capture, encryption-
  at-rest sweep, and clean-room fresh install (encrypted-from-birth DB, no
  migration). Full plan and results: docs/PHASE3_TEST_PLAN.md.


Phase 2 — Security hardening   (2026-07-14, commits 2a9ec37, 5e49c5f)
--------------------------------------------------------------------
Argon2id PIN hashing (BouncyCastle) with PBKDF2->Argon2id upgrade-on-verify;
SQLCipher-encrypted Room with plaintext->encrypted migration; brute-force
lockout with persisted counters; biometric unlock with PIN fallback;
foreground protection watchdog + boot receiver (a11y-revocation alert);
opt-in device-admin uninstall protection; FLAG_SECURE on non-debug builds.
Expanded to 48 unit tests.


Phase 1 — Core app-lock system   (commit 61f1db4)
-------------------------------------------------
Foreground-app detection via accessibility service, PIN authentication,
relock policies (immediate / grace / screen-off), and the settings and
app-list UI.
