# M1 (IS Phase 0) — Foundation Retrofit — Gate Review

- **Date:** 2026-08-17
- **Host / author:** NucBox G5 — GMKtec (on-host assistant), preparing the record for lead sign-off.
- **Against commit:** `d62b601` (+ this gate commit, which lands the WP8 matrix report, the reconciled RTM rows, the ADR-014/015 amendments, and this record together).
- **Baseline:** M1 began at `f7ffe29` (M0). Scope per `docs/process/M1_PLAN.md`; exit criteria per Implementation Strategy §5. RTM disposition reconciled to the post-ADR-019 v1.0.0 baseline per `docs/testing/WP8_GMD_MATRIX.md §7.2`.

## Exit criteria (M1_PLAN §Exit / Implementation Strategy §5)

- [x] **Automated builds green in CI** — `build` job success on `a66701c` ([Actions run 31989135009](https://github.com/PyrosCat/App_lock/actions/runs/31989135009)); `assembleProdRelease` (R8) + `assembleDevDebug` in the same job.
- [x] **Static analysis green (detekt + ktlint + Android lint + Konsist)** — same CI job (`testProdDebugUnitTest detekt … lint`; Konsist R1–R4 run inside the unit-test task). ADR-016.
- [x] **Dependency/license audit produced** — `generateLicenseReport` archived as the CI `license-inventory` artifact (`app/build/reports/dependency-license/`). FR-247 (`descoped-v1` as an FR; the inventory mechanism nonetheless runs). WP4.
- [x] **Zero service-locator lookups** — `core/Graph.kt` deleted in WP5 (`498a240`); Konsist **R1 terminal** (no `com.applock.core.Graph`, file must not exist) enforced in CI. ADR-015.
- [x] **Device gating-regression suite passes on the migrated build** — WP6 device exit (`docs/reports/campaigns/2026-08-11_wp6-device-exit_moto-g-2025.md`, OV-3/OV-4/F3 2/2 + ADR-018 upgrade drill) + WP5 matrix (`2026-08-09_wp5-matrix_nucbox-g5.md`).
- [x] **On-device smoke matrix (GMD) green** — NucBox `full` GMD **20/20** across API 26/29/30/33/35 (`docs/reports/campaigns/2026-08-17_wp8-gmd-matrix_nucbox-g5.md`) + CI `ci` group API 30/35 (`instrumentation` job success, run 31989135009). api29 heap caveat (§5.2) did **not** occur on the `aosp` image — Argon2 passed natively; no workaround needed.
- [x] **Real-hardware smoke pass (Moto G 2025, arm64 / API 35) green** — 4/4 (`docs/reports/campaigns/2026-08-16_wp8-smoke_moto-g-2025.md`).
- [~] **Docs synchronized: RTM · ADRs · changelog** — ADR-015 status line closed at the gate (GOVERNANCE §2.3 "Update Implementation status line", MAY-dated; classified in the changelog per §2.8) + index row (§2.6) ✅; **ADR-014 deliberately not edited** — NFR-COMP-001 status lives in `rtm.csv` and the WP8 reports, and ADR-014's matrix authority is its 2026-08-24 §2.7 note (restating either in the ADR would violate §2.7) ✅; changelog ✅; RTM: FR-228 `implemented-verified` (WP7) + the four retained rows (NFR-COMP-001, NFR-TEST-002, NFR-MNT-003, FR-234) → `partial` with evidence ✅. **Open:** the FR-026..080 `review` burndown (see below) is a gate-lead decision and is **not** yet applied.

## WP roll-up (all Closed, with evidence)

WP1 CI freeze · WP2 harness (`2026-07-22/23` reports) · WP3 static analysis/Konsist (ADR-016) · WP4 build variants (ADR-017) · WP5 Hilt (ADR-015; `2026-08-08/09`) · WP6 package realignment (ADR-001/011/018; `2026-08-11`) · WP7 DB migration hardening (R-004 Closed; `2026-08-16_wp7-migration-drills`) · WP8 instrumentation seed + GMD matrix (`2026-08-16` Moto G + `2026-08-17` NucBox) — all Closed.

## RTM disposition (reconciled to post-ADR-019 v1.0.0 — `WP8_GMD_MATRIX.md §7.2`)

- **Changed at this gate → `partial`** (retained rows, real M1 evidence): NFR-COMP-001 (WP8 matrix), NFR-TEST-002 (test seed + GMD + CI), NFR-MNT-003 (WP3 detekt/ktlint/Konsist), FR-234 (WP4 BuildConfig provenance).
- **Already done:** FR-228 `implemented-verified` (WP7), FR-229 `partial` (WP7).
- **Left `descoped-v1` (honoring ADR-019):** FR-226, FR-227, FR-247, FR-355, FR-356, FR-357, FR-358, FR-361, FR-363 — the M1 engineering qualities are carried by the retained NFRs, not their descoped FR twins (SRS v1.0.0 Appendix A §A.18). **Not** re-opened.
- **NFR-COMP-001 upgrade note:** the full matrix is now green (NucBox 20/20 + Moto G 4/4 + CI), so per the reconciliation's own criterion NFR-COMP-001 is **eligible for `implemented-verified`**; held at `partial` pending the lead's sign-off (a one-cell change if granted).

## Open item — FR-026..080 `review` burndown (GOVERNANCE §1.3.5)

33 rows in FR-026..080 remain `review` (empty implementation pointers). GOVERNANCE §1.3.5 makes the burndown part of the M1 gate, but the reconciliation (`§7.2`) flags **whether it is mandatory at this gate or a tracked follow-up as a governance-rule + gate-lead decision**, and it requires cross-checking each row against SRS v1.0.0 Appendix A §A.19 (several of the 33 may themselves be reserved/descoped) and classifying against the ADR-013B/019-narrowed v1.0.0 meanings. **Not** performed here (it is a lead-scope judgment, not a NucBox device task). This is the sole outstanding gate item.

## Decision

**M1 EXIT: PENDING — lead sign-off.** All technical exit criteria are met and green (builds, static analysis, license audit, service-locator elimination, device gating regression, GMD smoke matrix, real-hardware smoke). The only outstanding item is the FR-026..080 `review` burndown disposition (do-now vs. tracked follow-up), which is the lead's call. Recommended on grant: resolve the burndown disposition, optionally promote NFR-COMP-001 → `implemented-verified`, then sign **M1 EXIT: GRANTED — \<name/host\>, 2026-08-17**. **Next milestone: M7 (accessibility/detection exit).**
